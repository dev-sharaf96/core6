/*
 * Represent Najm status model
 */

export interface INajmStatus {
  // Code
  code: string;
  // najm status in arabic
  nameAr: string;
  // najm status in english
  nameEn: string;
  name: string;
}
