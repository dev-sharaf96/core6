export class samaMegaReportFilter {

    // StatusCode: number;
 // StartDate: Date;
 // EndDate: Date;
 constructor(
   public IsExcel: number | null = null,
   public StartDate?: string,
   public EndDate?: string,

 ) {
   // this.StatusCode = StatusCode || null;
   // this.StartDate = StartDate || null;
   // this.EndDate = EndDate || null;
 }
  
}